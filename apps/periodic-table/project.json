{
  "name": "periodic-table",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/periodic-table/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/periodic-table"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "periodic-table:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "periodic-table:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "periodic-table:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "periodic-table:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "periodic-table:build:development"
        },
        "production": {
          "buildTarget": "periodic-table:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["coverage/apps/periodic-table"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/periodic-table"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/periodic-table/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "serve-static": {
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "periodic-table:build"
      }
    }
  },
  "tags": []
}
